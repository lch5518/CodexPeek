# CodexPeek Design Specification: Native Allowance View

> 확정일: 2026-09-17. 사용자가 선택한 **design-harness 버전**을 제품 디자인 기준으로 채택한다.
> 대상: Windows 10/11 x64, Rust 2021 + Win32/GDI 네이티브 앱.
> 적용 지침: 사용자가 제공한 `frontend-design` 및 `redesign-existing-projects` 스킬.

이 문서는 호버 사용량 화면, 우클릭 메뉴, 프로필 관리의 확정 설계다.
구현 기준은 `target/design-harness/source`, 확인한 실행 파일은
`target/design-harness/release/codex-peek.exe`다. 루트 `src/`로의 코드 통합과 배포는 별도 작업이며,
이 문서 수정으로 완료된 것으로 간주하지 않는다. `target/`은 로컬 생성물이므로 아래 사양을
자체적으로 읽고 구현할 수 있어야 한다.

## 1. 제품의 역할과 디자인 원칙

**“어느 사용량 창에 얼마가 남았고, 언제 초기화되는지 같은 자리에서 확인한다.”**

- 각 사용량 창의 이름·비율·막대·초기화 시각을 하나의 그룹으로 묶는다. 두 창의 비율은 같은 크기로 한 번씩 표시한다.
- 시스템 글꼴, 불투명한 중립 배경, 제한된 여백과 명확한 숫자 정렬을 사용한다.
- 파랑은 선택·포커스·주 작업에, 초록·주황·빨강은 사용량 상태에 사용한다. 서로 다른 의미를 같은 강조로 표현하지 않는다.
- 읽기 전용 정보와 실행 가능한 명령을 구분한다. 기존 메뉴·프로필·폴링·복구 동작을 유지한다.
- 실제 문자열을 측정해 배치한다. 빈 데이터, 긴 번역, 좁은 작업영역을 정상 상태와 함께 설계한다.

| 화면 | 사용자가 확인하거나 수행하는 일 | 유지할 범위 |
| --- | --- | --- |
| 작업표시줄·플로팅 위젯 | 사용량과 상태를 짧게 확인 | 기존 두 창 표시·축약·이동·표시/숨기기·Explorer 복구 |
| 호버 상세 | 각 창의 비율, 초기화, 소비 속도, 예측, 최근 토큰 추이 확인 | 비활성·읽기 전용 팝업 |
| 우클릭 메뉴 | 프로필 전환, 새로 고침, 설정, 진단 등 기존 명령 실행 | 네이티브 메뉴 계층·체크·키보드·하위 메뉴 |
| 프로필 관리 | 편집할 프로필 선택, 이름 변경, 로그인·로그아웃·삭제 | 기존 입력·확인·취소·비활성 정책 |

작업표시줄 호스트는 자체 Win32 위젯이다. OS 작업표시줄 버튼의 썸네일이나 Windows Widgets
보드를 대체하지 않는다. 트레이만 사용하는 경우 항상 보이는 텍스트를 보장하지 않는다.
새 웹 프레임워크, 웹 폰트, 장식용 이미지, 그라데이션, 모조 Acrylic/Mica 효과는 추가하지 않는다.

## 2. 호버 사용량 화면

모든 치수는 **DIP(96 DPI 논리 픽셀)** 기준이다. HWND·GDI에는 해당 모니터 DPI로 변환한
물리 픽셀을 전달하며 두 좌표계를 섞지 않는다.

### 치수와 배치

| 항목 | 확정 기준 |
| --- | --- |
| 기본 너비 | 400 DIP. 모니터 작업영역에 맞춰 제한 |
| 높이 | 문자열·데이터에 따라 계산. 최대 `min(560 DIP, 작업영역 높이 - 24 DIP)` |
| 화면 여백 / 앵커 간격 | 최소 12 DIP / 8 DIP |
| 내부 여백 | 20 DIP. 좁은 폭에서도 같은 여백 유지 |
| 바깥 형태 | 반경 8 DIP, 테두리 1 DIP, Windows 기본 그림자 경로 |
| 사용량 행 | 창 이름은 시작 쪽, 비율은 끝 쪽. 비율 영역 108 DIP, 사이 간격 12 DIP |
| 좁은 폭 | 320 DIP 미만이면 행 안의 이름·비율도 세로로 쌓음. 320 DIP부터 같은 행에 배치 |
| 사용량 막대 | 높이 4 DIP. 숫자 아래 6 DIP, 막대 아래 초기화 문구까지 8 DIP |
| 행 사이 간격 | 초기화 문구 뒤 16 DIP |
| 소비 속도·예측 영역 | 본문 전체 폭의 보조 면 한 개, 내부 여백 12 DIP |

패널 본문은 전체 폭을 쓰는 단일 열이다. 창별 이름과 숫자의 좌우 정렬은 행 내부의 배치다.
폭을 줄이면 텍스트를 다시 측정한다. 높이가 부족하면 소비 속도의 보조 설명과 토큰 그래프를
생략하는 축약 배치를 사용한다. 비율·초기화·상태·예측은 보존한다. 그래도 맞지 않거나
사용 가능한 폭이 200 DIP 미만이면 기존 표준 툴팁으로 폴백한다. 비트맵을 잘라서 맞추지 않는다.

### 읽는 순서

1. 프로필명과 현재 표시 모드(`남은 사용량` 또는 `현재 사용량`), 필요할 때 상태 안내.
2. Primary → Secondary 순서의 사용량 행. 존재하는 창만 표시한다.
3. 제공되는 초기화 추가 정보와 소비 속도·예측 영역.
4. 일별 토큰 사용량 그래프와 날짜.
5. 얇은 구분선 아래 마지막 성공 시각.

```text
╭────────────────────────────────────────╮
│ 개인 프로필                            │
│ 남은 사용량                            │
│                                        │
│ 5시간                             88%  │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━───────   │
│ 초기화 시각: 2026-09-17 18:30           │
│                                        │
│ 7일                               66%  │
│ ━━━━━━━━━━━━━━━━━━━━━──────────────   │
│ 초기화 시각: 2026-09-20 10:00           │
│                                        │
│  사용 속도 여유                        │
│  최근 2시간 동안 2% 사용               │
│  사용량 소진 예측                      │
│  예측에 필요한 표본을 수집 중          │
│                                        │
│ 일별 토큰 사용량                       │
│ ▁ ▃ ▂ ▅ ▂ ▁ ▄ ▆ ▂ ▃ ▅ ▂ ▄ ▂           │
│ 09/04             09/11          09/17  │
│ ────────────────────────────────────── │
│ 마지막 성공 120초 전                   │
╰────────────────────────────────────────╯
```

예시는 가상 데이터다. 기간 이름·초기화 시각·문구는 기존 현지화된 표시 모델을 사용한다.
모든 계정의 창을 5시간·7일로 고정하지 않는다. 프로필명을 헤더의 중심으로 두며 별도의
대문자 브랜드 라벨, 주간 비율의 중복 헤드라인, 굵은 헤더 괘선을 추가하지 않는다.
팝업에 닫기·새로 고침 등 조작 버튼을 그려 넣지 않는다.

### 소비 속도와 그래프

소비 속도 요약과 보조 설명을 먼저, 기존 Secondary 창의 소진 예측을 그 아래에 둔다.
예측 수집 중·관측 부족·오래됨·비활성 등 기존 상태를 유지하며 초기화 시각을 예측으로 바꾸지 않는다.

그래프는 CLI가 제공한 최근 최대 14일의 `daily_token_usage`를 사용한다. 사용률 증가 기록인
`daily_usage`를 토큰 수로 바꾸지 않는다. 중립색 막대와 날짜를 사용하며 클릭 가능한 차트처럼
강조하지 않는다. 그래프 높이는 53 DIP, 날짜 영역은 16 DIP, 사이 간격은 4 DIP다.
0 토큰인 날은 기준선만 보이고, 날짜는 대응하는 막대에 정렬한다. 세 날까지는 모든 날짜,
그보다 많으면 처음·중간·마지막 날짜를 표시한다. 이력이 없으면 그래프를 생략한다.

## 3. 우클릭 메뉴

| 항목 | 기준 |
| --- | --- |
| 명령·하위 메뉴 항목 | 높이 32 DIP, 텍스트 13 DIP |
| 읽기 전용 정보 | 높이 40 DIP, 보조 면으로 구분. 클릭해도 명령을 실행하지 않음 |
| 구분선 | 높이 9 DIP 영역 안에 얇은 선, 좌우 여백 16 DIP |
| 너비 | 실제 글꼴로 레이블 폭을 측정하고 체크·아이콘·화살표 여백을 포함. 220~520 DIP 범위 |
| 호버·키보드 하이라이트 | 파란 선택 면. 좌우 4 DIP, 상하 2 DIP 여백. 별도 가장자리 강조선 없음 |

현재 값의 체크 표시는 파란 강조색으로, 하위 메뉴는 방향 화살표로 구분한다.
하이라이트는 현재 가리키는 항목, 체크는 적용된 값이라는 서로 다른 의미를 유지한다.
비활성 항목은 네이티브 비활성 동작과 보조 텍스트를 사용한다.

프로필 전환과 프로필 관리 명령, 새로 고침·설정·진단·업데이트·종료의 기존 그룹을 유지한다.
아이콘·체크·화살표와 레이블의 정렬을 맞추고 RTL에서는 방향을 반전한다. 명령 ID와 동작은
스타일 변경으로 바꾸지 않는다. 키보드 이동·하위 메뉴 열기·Esc 닫기는 네이티브 메뉴가 처리한다.
고대비·스크린리더 환경에서는 기존 기본 메뉴 폴백을 유지한다.

## 4. 프로필 관리

목록에서 고른 **편집 대상**과 위젯에 **표시 중인 프로필**을 구분한다. 편집 대상은 선택 면과
가장자리 표시로, 실제 표시 중인 프로필은 기존 현지화된 텍스트 표식으로 나타낸다.
목록 선택만으로 표시 프로필이 바뀐 것처럼 표현하지 않는다.

| 항목 | 기준 |
| --- | --- |
| 기본 클라이언트 폭 / 바깥 여백 | 620 DIP / 16 DIP. 작업영역과 번역 측정 결과에 맞춰 조정 |
| 목록 | 행 높이 94 DIP, 기본 3행 공간. 높이가 부족하면 행 단위로 줄임 |
| 행 내용 | 이름·현재/기본 프로필 표식 → 로그인 이메일 → 사용량 요약 → 부가 정보 → 상태 막대 |
| 선택 | 파란 선택 면 + 시작 가장자리 3 DIP 표시. 키보드 포커스는 별도 네이티브 표시 |
| 막대 | 높이 3 DIP, 기존 사용률·상태색 의미 유지 |
| 컨트롤 | 최소 높이 32 DIP. 작업 버튼은 측정한 문구 폭 + 좌우 총 24 DIP, 최소 너비 72 DIP |
| 배치 | 목록 아래 `+`, 이름 레이블과 입력, 이름 변경·로그인·로그아웃·삭제 순서 |

이름은 16 DIP의 중간 굵기, 본문은 14 DIP다. 선택 행에서도 이름·이메일·상태를 읽을 수 있는
대비를 유지한다. 로그인·추가의 주 작업 버튼은 파란 강조색과 대비되는 텍스트를 사용한다.
다른 버튼의 네이티브 비활성·포커스·눌림 동작을 유지한다. 버튼 문구는 줄바꿈하거나 잘라내지
않으며 한 행에 맞지 않으면 버튼 단위로 두 번째 행에 배치한다.

`+`는 최대 8개 프로필 제한과 작업 중 비활성 정책을 따른다. 프로필 추가 창은 기존 추가·취소
흐름을 사용한다. 관리자 창의 닫기·Esc, 입력창의 Tab 순서, 네이티브 Enter 처리, 작업 중
중복 명령 방지, 작업 후 버튼 복구를 유지한다. 삭제 확인과 시스템 프로필의 삭제·로그아웃 제한을 보존한다.

이메일은 `account/read`에서 검증된 `AccountEmail`만 메모리에서 표시한다. 누락·실패·오래된
조회에서는 기존 안전한 대체 표시를 사용하며, RTL에서도 이메일 자체는 LTR로 읽히게 한다.
호버 패널이나 로그·파일로 이메일 표시 범위를 넓히지 않는다.

## 5. 공유 색상

Windows 시스템 테마를 따른다. 아래 값은 sRGB이며, Win32 `COLORREF`로 전달할 때 바이트
순서를 변환한다. 팝업·메뉴·프로필의 색상은 `DialogPalette`와 `popup_palette`의 역할을 공유한다.

| 역할 | Light | Dark |
| --- | --- | --- |
| 기본 배경 | `#FAFAFA` | `#242424` |
| 보조 면·입력 면 | `#FFFFFF` | `#292929` |
| 선택 면 | `#E5EFFB` | `#2C3C4C` |
| 바깥·제어 테두리 | `#ADADAD` | `#6B6B6B` |
| 장식용 구분선 | `#E4E4E4` | `#3E3E3E` |
| 기본 텍스트 | `#1F1F1F` | `#F5F5F5` |
| 본문 보조 텍스트 | `#454545` | `#D0D0D0` |
| 작은 보조 정보 | `#5E5E5E` | `#B0B0B0` |
| 작업·포커스 | `#005FB8` | `#99CCFF` |
| 주 작업 버튼 텍스트 | `#FFFFFF` | `#242424` |
| 오류 텍스트 | `#B42318` | `#FF9A90` |
| 정상·주의·위험 막대 | `#48C774` / `#F5A623` / `#FF5C5C` | 동일 |

프로필 막대의 트랙은 해당 행 표면에 검정/흰색을 36/255 불투명도로 합성한다.
버튼 호버·눌림은 기존 오버레이를 합성하며 호버 13/255·눌림 23/255(밝음),
호버 15/255·눌림 26/255(어두움)를 사용한다. 호버 팝업의 막대 트랙은 장식용 구분선 색이다.

작은 텍스트는 실제 배경 조합에서 4.5:1, 큰 텍스트와 의미 있는 제어·포커스 경계는 해당
3:1 기준을 확인한다. 장식용 선과 상태 막대의 색만으로 의미를 전달하지 않는다.
정상색을 일반 버튼이나 선택 면의 브랜드 장식으로 사용하지 않는다.

## 6. 타이포그래피와 정렬

| 용도 | 크기 | 굵기 | 배치 기준 |
| --- | --- | --- | --- |
| 호버 프로필명 | 18 DIP | 600 | 실제 측정, 최소 블록 높이 24 DIP |
| 사용량 비율 | 28 DIP | 600 | 끝 쪽 정렬, 최소 블록 높이 34 DIP |
| 창 이름·소비 속도 요약 | 14 DIP | 600 | 최소 블록 높이 20 DIP |
| 예측 본문·오류 안내 | 13 DIP | 400 | 최소 블록 높이 19 DIP |
| 표시 모드·초기화·보조 정보 | 12 DIP | 400 | 최소 블록 높이 18 DIP |
| 그래프 날짜 | 11 DIP | 400 | 16 DIP 영역, 막대와 정렬 |
| 메뉴 | 13 DIP | 400 | 단일 행 |
| 프로필 이름 / 본문 | 16 / 14 DIP | 500 / 400 | 고정 목록 행 안에서 역할별 정렬 |

Segoe UI Variable·Segoe UI 계열과 Windows의 한글·다국어 글꼴 대체를 사용한다.
숫자·날짜도 같은 시스템 산세리프 계열로 읽히게 한다. 장식용 모노스페이스, 대문자 라벨,
강한 음수 자간은 추가하지 않는다. 위 블록 높이는 고정 잘림 상자가 아니라 측정 높이의 하한이다.

긴 문구는 실제 GDI 글꼴로 측정하고 필요한 높이를 확보한다. 비율·단위·초기화·오류를
말줄임으로 숨기지 않는다. 이름과 이메일처럼 목록에서 축약하는 텍스트도 기존 접근 가능한
행 텍스트를 보존한다. 기존 12개 언어와 RTL 배치를 유지하고 문구는 `localization.rs`에서 관리한다.

## 7. 데이터와 상태의 의미

| 입력·상태 | 표시 계약 |
| --- | --- |
| `primary`, `secondary` | 존재하는 창만 순서대로 표시. 사용량 갱신으로 두 행의 순서를 바꾸지 않음 |
| `display_percent`, `percent_text` | 선택한 남음/사용 모드의 숫자와 막대 길이. 길이는 0~100%로 제한 |
| `used_percent` | 상태색 기준. 남음/사용 모드를 전환해도 위험 단계가 바뀌지 않음 |
| `reset_text`, `reset_credits_text` | 해당 창 아래 초기화, 제공된 추가 정보. 없는 값을 추정하지 않음 |
| `consumption_pace`, `forecast` | 소비 속도와 소진 예측의 독립된 의미 유지. 수집 중·오래됨·꺼짐 등 기존 상태 보존 |
| 첫 로딩·사용량 없음 | 실제 상태 문구와 미확정 값 `—`. 0%·100%를 만들어 표시하지 않음. 로딩은 본문색 |
| 캐시 갱신·실패 | 같은 프로필의 마지막 정상 값과 성공 시각 유지. 실패는 안전한 오류 문구와 오류색 |
| 오래된 정보 | 지연 상태를 명시. 원인을 모르는 실패를 무조건 오프라인이라고 표현하지 않음 |
| 일부 창·토큰 이력 없음 | 없는 행이나 그래프만 생략. 다른 단위의 데이터로 대체하지 않음 |

호버·프로필 막대의 `ProfileUsageStatus`는 사용률 `<70` 정상, `70~<90` 주의, `≥90` 위험이다.
작업표시줄 `TaskbarRisk`의 기존 단계와 소비 속도·조회 상태를 나타내는 `TaskbarIndicator`는
각자의 의미를 유지한다. 전역 `UsageLevel`의 별도 분류를 UI 스타일 변경으로 합치지 않는다.
프로필을 바꿀 때 다른 프로필의 캐시를 새 이름 아래에 표시하지 않는다.

## 8. 입력·수명·Windows 동작

호버는 기존 Windows 툴팁 알림에 연결된 읽기 전용 창이다. `WS_EX_NOACTIVATE`,
`WS_EX_TRANSPARENT`, `SW_SHOWNA`를 유지해 다른 앱의 키보드 포커스를 가져오지 않는다.
위젯 이탈·툴팁 종료·소유 창 정리 시 닫는다. 표시부에서 패널로 포인터를 옮겨 조작하는
동작이나 별도 Interactive 상태는 이번 확정 범위에 포함하지 않는다.

호버 진입으로 로그인·새로 고침·프로필 변경·추가 RPC를 실행하지 않는다. 명시적인 작업은
기존 우클릭 메뉴와 네이티브 대화 상자를 통해 수행한다. 별도 열림·닫힘 애니메이션이나
250/300ms 유지 타이머를 새 기준으로 도입하지 않는다.

위치는 실제 위젯 HWND가 속한 모니터의 작업영역을 사용한다. 앵커 위쪽을 우선하고 공간이
없으면 아래쪽 또는 작업영역 안에 배치한다. DPI·작업표시줄 자동 숨김·Explorer 재시작에
대한 기존 복구와 트레이·플로팅 폴백을 유지한다. 단일 인스턴스와 기존 설정 저장 정책도 유지한다.

## 9. 접근성·데이터 경계

- 고대비·스크린리더 환경 또는 렌더링 실패에서는 기존 표준 툴팁·메뉴·행 텍스트 폴백을 유지한다. GDI 화면을 새 조작 가능한 dialog처럼 선언하지 않는다.
- 메뉴·프로필의 키보드 이동, 포커스, 선택·체크·비활성 표시를 보존한다. 전체 OS·보조 기술 검증은 시각 캡처와 구분한다.
- 사용량·로그인 상태는 제한된 Codex CLI `app-server` 경로로만 얻는다. `auth.json`을 읽거나 파싱하지 않는다.
- 토큰·계정 ID·원본 RPC·프록시 값은 UI·로그·fixture에 보관하지 않는다. 문서와 테스트 예시는 가상 데이터 및 `.invalid` 이메일만 사용한다.
- UI 스레드의 장시간 I/O를 금지하고 기존 워커·`UiBackend`·프로세스 정리·요청 제한을 유지한다. 수동 갱신 10초 쿨다운과 실패 백오프 1/2/4/8/15분을 변경하지 않는다.

## 10. 구현 경계와 검증

아래 경로는 제품 루트 기준의 책임이다. 선택된 구현은 현재 `target/design-harness/source/`의
같은 경로에 있으며, 루트 소스에 통합할 때 기존 기능 변경과 섞지 않는다.

| 위치 | 책임 |
| --- | --- |
| `src/windows/popup.rs` | 표시 모델, 400 DIP 너비, 팔레트 매핑, 배치·접근성 폴백 |
| `src/windows/native/usage_popup.rs` | 행 중심 측정·GDI 그리기, HWND와 GDI 자원 수명 |
| `src/windows/design.rs` | 공유 팔레트, 프로필 레이아웃·DPI 토큰 |
| `src/windows/tray/platform.rs`, `src/windows/profile_dialog/platform.rs` | 네이티브 메뉴·프로필 표시와 입력 계약 |
| `src/windows/native/platform.rs`, `taskbar*`, `widget.rs`, `lifecycle.rs` | 기존 이벤트·툴팁·작업표시줄·복구 경로 |
| `src/domain.rs`, `src/localization.rs`, `src/app.rs`, `src/windows/mod.rs` | 사용량 의미, 현지화, 안전한 UI 스냅샷과 액션 경계 |

### 선택된 빌드에서 확인한 증거

- 포맷 검사, 전체 테스트 522개, Clippy 경고 금지 검사, 별도 릴리스 빌드 통과.
- 실제 Windows 프로필 창 테스트 1개를 별도 실행해 작업 중 비활성화 → 갱신 후 활성화 → 닫기를 확인.
- 한국어·영어·RTL, 100/125/150/175/200% DPI, 280/320/400 DIP 폭의 측정·배치 검사와 밝은·어두운 GDI 렌더링 확인. 모든 조합을 사람이 수동 조작한 것은 아님.
- 비율과 해당 초기화의 배치, 표시 모드와 독립된 위험색, 로딩·오류, 0 토큰과 날짜 정렬 검사 통과.
- Windows 10/11 전체 조합, 다중 모니터, 자동 숨김, Explorer 재시작, 텍스트 확대·보조 기술 및 실제 계정 로그인 흐름의 종합 수동 검증은 남아 있음.

로컬 비교 자료는 `target/design-harness/comparison.html`과 `screenshots/`, 상세 기록은
`target/design-harness/README.md`에 있다. 이 경로들의 파일은 Git 추적 문서나 배포 자산이 아니다.

### 통합·릴리스 확인 기준

- [ ] 호버에서 두 창의 비율과 각 초기화 시각을 함께 읽을 수 있고 숫자·단위가 중복·잘림 없이 표시된다.
- [ ] 메뉴의 하이라이트·체크와 프로필의 편집 선택·표시 중 표식이 구분된다.
- [ ] 로딩·오류·오래됨·없는 이력에서 기존 데이터 의미와 안전한 액션 경로가 유지된다.
- [ ] [릴리스 체크리스트](docs/RELEASE_CHECKLIST.md)에 따라 DPI·테마·키보드·보조 기술·다중 모니터·자동 숨김·Explorer 복구를 수동 확인한다.
- [ ] 루트 소스 통합 후 포맷·테스트·Clippy·릴리스 빌드를 실행하고 README 이미지를 실제 채택된 화면과 맞춘다.

문서만 수정할 때는 선택된 소스와의 사양 대조 및 `git diff --check`로 검증한다.
실제 구현을 통합하거나 배포할 때는 다음 검사를 적용한다.

```powershell
cargo fmt --all -- --check
cargo test --all-targets
cargo clippy --all-targets --all-features -- -D warnings
cargo build --release
git diff --check
```

## 구현자용 한 줄 브리프

> 시스템 테마와 Segoe UI 계열을 사용하는 400 DIP Windows 네이티브 사용량 패널을 만든다. 각 창의 비율·막대·초기화를 같은 그룹에 한 번씩 표시하고, 소비 속도·예측·토큰 그래프·마지막 성공 시각을 세로로 잇는다. 파란 선택·작업 표시와 사용량 상태색을 구분하며, 호버는 비활성·읽기 전용, 메뉴와 프로필 관리는 기존 네이티브 입력·접근성·복구 계약을 유지한다.
